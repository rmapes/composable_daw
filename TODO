x 1. Select and deselect patterns

x 2. Add Menu with File/New and Edit/Delete Pattern
. Implement file new
. Implement Edit delete pattern

3. Allow creation of pattern at play head
   . implement play head

x - display across all tracks, initialised to zero
x - implement track tick ruler

x - allow playhead location to be selected by clicking on tick ruler
x - allow playhead to follow along while playing. stays where it is when stopped
x - add stop button

- clicking on stop while stopped should move playhead to start
- If track continues outside display area, playhead reaching a predefined point (mid point of tick ruler?) should start scrolling track rather than moving playhead
- allow add pattern to create at pattern at play head or next available space (need to make this logic configuirable)
- add bar number to tick ruler

3. Allow deletion of pattern via delete key
   . Use context to determine what should be deleted?

4. Allow multi pattern select

VVVVVVVV
ARCHIVE

Get Basic functionality working again

x- Start track with a default pattern added and selected
x- Show pattern editor if track has selected region
x- Allow patterns to be edited and saved
x- Add play button to control bar

Bugs

x - Fix PPQ vs ticks per second discrepancy

Multi track functionality

x - Add Track button in composer window
x - Allow track selection by clicking

Tech Debt

x- Replace debug println with log framework

- Add unit tests
- Minimize use of traits vs concrete types
- Eliminate Box wherever possible
  x- Eliminate Option for Event Stream
- Minimize use of into
  x- Delete slint files
- Move ppq into a shared settings object
- Rationalise naming of length_in_ticks vs get_length_in_ticks
  x- Rationalise EventTime and Tick

Make functional

- Save and load
- Allow bpm to be changed
- Allow instrument to be selected

Multi region

- Allow pattern regions to be added at any point on timeline
- Allow midi regions to be added

Design kit improvements

- add icon library

Prettify design

Cleanup

- Remove fixed beats per quarter note from pattern (provide a length for the pattern)
- Minimise size of all ints (convert u32 into smallest size that covers the range)
- Minimise unnecessary copies

Planning:

We need the following threads:
ui
audio
general processing
(possibly an autosave thread)

slint automatically generates a separate ui thread, so we can keep general processing on the main thread, and have a separate thread for audio, controlled by an AudioController class. (which wraps notifies).

ui and general can wait, i.e. can allocate memory and run locks.
audio thread must be real time, so cannot allocate memory during play loops (but can do so outside of these loops).

How to manage state:

- copies to keep state in sync:
  - multi write: write to all states every time they change
  - replace:
    - copy from: copy from core state before any major actions (could be costly)
    - copy to: copy to substates after every update block
  - sync:
    - align at critical points.

Multi write seems the best option.
Probably best done via a central state object, whose class manages multi way updates.
However, this may make it difficult to avoid locks in the audio thread. Could investigate whether try_lock would resolve this.

State required:
Audio:
We need io from busses in order to plot volumes + spectrographs
We need the routing of midi generators, audio generators, midi in, audio in, and audio out.
Each generator will require state. For programmatic state (vs midi in and audio in) we can get away with taking a copy of the state on play.

Ideal solution:
Main thread creates a slint based Data Model containing
Output Generators (Audio / Midi)

- Tracks
- Regions (can audio and midi be mixed?)
  - Midi
    - Sequence
    - Pattern
  - Audio
    - Sequence
    - Pattern
- Busses
- FX
- Routing
  - midi outputs to audio generators
  - audio generators to busses
  - audio generators to FX
  - buss to stereo out
  - (inputs to generators / recorders)
- General / Settings

Note, inputs/recording are too complicated right now. Ignore this and build a good tracker.

The audio thread, on the other hand, will maintain a set of owned buffers containing audio data for each of these structures.
TBD whether to trigger a rebuild of this structure on hitting play, or based on updates to the Structure definition. Probably the latter, managed with notifications to an audio side wiring manager
In addition, each Buss will expose a metrics section containing atomic variables for metric values - initally left and right channel value.

Visualisers can than calculate a display value from this by reading it on the graphics event loop. Additional metrics may be calculated by other threads.

What do we need for layout design kit:

Palette:
Rack colour: colour of components simulating raised metal framework, that panels are inset into
Module colour: colour of a pluggable module (e.g. channel strip with embedded controls and display)
Display colour: colour simulating a display screen (e.g. meters, or led displays)
Control colour

Rack

- Top level container included Top command ribbon and transport control. Simulates a modular audio device that other modules can be mounted into.
- Should look like a stamped metal case, with holes through which the modules can be seen. This can be achieved by setting the background colour of
- the container to RACK and implementing all module slots to use RACK_RECESS to simulate an inset bevelled edge.
- Note the illusion will be slightly broken by having resizable module slots. However, this is an acceptable trade off for the configurability.

Module Slot:

- A container for a module.
- In the first instance, will be fixed to contain specific modules, but in the future new slots can be created, moved and resized, and different modules allocated to different slots.

Module (channel strips, composer window, editor window)

- Should look like a slightly recessed audio unit with casing. To help with the inset effect, the MODULE case colour should be slightly darker than RACK, although Module Slot can take care of the recessing itelf.
- Modules can contain Controls, Labels and Displays
