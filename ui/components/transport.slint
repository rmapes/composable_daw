
import { Button } from "std-widgets.slint";

export global TransportActions {
    callback play_midi();
}

component PlayButton inherits Button {
    width: 100px;
    height: 40px;
    text: "Play";
    clicked => TransportActions.play_midi();
}

export component Transport {
    PlayButton {}
}
