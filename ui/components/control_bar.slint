import { HorizontalBox } from "std-widgets.slint";
import { Transport } from "transport.slint";
import { TopNav } from "../designkit/containers.slint";
export component ControlBar inherits TopNav {
    HorizontalBox {
        Transport {} 
    }
}